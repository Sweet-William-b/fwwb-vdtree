# A28 后端方案说明

本文档用于说明 A28 比赛版本的后端设计方案。它不是接口手册，也不是开发日志，而是一份面向实现和联调的后端说明文档。

接口契约、请求参数、响应 JSON、状态字段定义请统一以 [接口.md](./接口.md) 为准。本文档重点回答三个问题：

1. 当前后端已经做到什么程度
2. 双视频输入后端为什么要这样设计
3. 后端接下来应该如何实现与验收

---

## 1. 目标与边界

### 1.1 目标

本次后端目标是把当前仓库升级为一个可比赛演示的双输入视频分析后端，正式支持两类输入：

- 普通视频上传
- RTSP 在线视频

两类输入统一遵循同一套处理规则：

- 基础段长度固定为 `5` 帧
- 第 `1` 个基础段只缓存，不触发识别
- 从第 `2` 个基础段开始，用“上一段 5 帧 + 当前段 5 帧”组成一个 `10` 帧分析窗口
- 每个窗口执行一次识别
- 窗口结果聚合为事件
- 事件进入 SSE、导出、历史、报告闭环

### 1.2 边界

本方案只讨论后端，不讨论前端页面细节，不讨论 UI 样式，不讨论比赛讲解文案。

本方案默认：

- 正式输入只支持 `upload + rtsp`
- `sample` 模式仅保留为开发调试和离线兜底能力，不作为主业务链路
- 不引入数据库
- 不迁移到新框架
- 不重做整个 `campus_demo` 结构

### 1.3 非目标

以下内容不属于本次后端主目标：

- 设计新的前端框架
- 引入多路视频调度平台
- 迁移到数据库存储
- 改写为完整微服务架构
- 支持 HTTP/HLS 直链在线视频
- 重建整套训练系统

---

## 2. 当前现状

### 2.1 现有后端入口

当前后端入口在 [VADTree/campus_demo/app.py](./VADTree/campus_demo/app.py)。

它已经具备一套基础可用的文件服务 + API + SSE 结构，核心能力包括：

- 上传接口
- 任务创建接口
- 单任务查询接口
- SSE 状态流接口
- 事件查询接口
- 事件编辑接口
- 导出接口
- 历史任务接口
- 健康检查接口

### 2.2 已有能力

当前后端已经完成的部分，主要是“工程外壳”和“离线结果复用能力”：

- 可以上传本地视频并落盘
- 可以创建任务并维护基础状态
- 可以通过 SSE 推送状态
- 可以读取已有缓存结果，生成事件、报告和导出物
- 可以编辑事件并刷新当前版本日志
- 可以导出 JSON、CSV、切片清单、ZIP
- 可以保留历史任务并支持回看

### 2.3 当前缺口

当前版本距离双输入真实分析后端，还存在两个关键缺口：

#### 普通视频缺口

当前上传视频虽然能落盘，但分析流程仍主要依赖已有缓存结果加载，而不是对这个新上传视频本身做真实分析。

这意味着：

- 上传文件已经进入系统
- 但上传后不一定真正触发完整视频读取、分段、窗口识别
- 当前更多是“已有结果展示型后端”，不是“新视频真实分析型后端”

#### RTSP 缺口

RTSP 输入目前仍然是占位能力。在当前 `app.py` 中，RTSP 分支创建任务后并不会真正拉流推理，而是直接报“接口已预留但尚未接入实时流推理”。

这意味着：

- 任务模型里已经有 `rtsp` 分支
- 前端也已经预留了 RTSP 输入表单
- 但后端没有真正的 RTSP 连接、解码、录制、切段、窗口分析、重连和收尾逻辑

### 2.4 当前系统定位

因此，当前系统的真实定位应当表述为：

> 已有一个可运行的比赛后端骨架，但双输入中的“上传真实分析”和“RTSP 在线分析”还没有落地为完整后端链路。

---

## 3. 目标架构

为了同时支持普通视频和 RTSP 在线视频，并且保持导出、历史、事件结构一致，后端采用统一的分层结构：

```text
输入接入层
  -> 视频切段与窗口构造层
  -> 窗口识别层
  -> 事件聚合层
  -> 任务状态 / SSE 层
  -> 导出与历史层
```

### 3.1 输入接入层

职责：

- 接收上传视频
- 接收 RTSP 地址
- 创建统一任务对象
- 管理任务初始状态
- 为后续分析链路准备输入上下文

输入接入层只负责“接入”和“调度”，不直接负责识别。

### 3.2 视频切段与窗口构造层

职责：

- 按规则切出基础段
- 管理“上一段 / 当前段”缓冲
- 生成统一的 `10` 帧分析窗口
- 维护当前时间点和段计数

这一层是双输入统一规则的核心。无论是上传视频还是 RTSP，最终都必须在这一层被规整为同一种窗口输入。

### 3.3 窗口识别层

职责：

- 接收 `10` 帧窗口
- 调用 `VADTreeWindowRunner`
- 输出窗口级识别结果：
  - `confidence`
  - `behavior_type`
  - `reason_text`

这一层不直接暴露给前端，它服务于事件生成。

### 3.4 事件聚合层

职责：

- 把单窗口结果转成事件片段
- 对时间连续、类别一致的窗口进行合并
- 生成统一 `AlertEvent`
- 维护最新事件列表

这一层决定后端“报什么、怎么报、多久报一次”。

### 3.5 任务状态 / SSE 层

职责：

- 维护任务生命周期
- 实时更新运行态指标
- 通过 SSE 把状态和事件推给前端

这一层是前后端交互体验的核心。

### 3.6 导出与历史层

职责：

- 输出当前事件版本
- 生成报告
- 导出 JSON / CSV / 切片 / ZIP
- 写入历史任务记录

这一层保证比赛演示的闭环能力。

---

## 4. 核心设计

## 4.1 普通视频处理流程

普通视频链路是“完整读取视频后顺序分析”的离线任务。

完整流程如下：

1. 前端上传视频，后端落盘，生成 `source_id`
2. 前端基于 `source_id` 创建 `upload` 任务
3. 后端打开该视频文件，读取总帧数、原始 FPS、总时长
4. 按顺序每 `5` 帧切出一个基础段
5. 第 `1` 段只缓存，不识别
6. 从第 `2` 段开始，使用“上一段 + 当前段”组成 `10` 帧窗口
7. 每个窗口送入 `VADTreeWindowRunner`
8. 识别结果送入事件聚合器
9. 事件列表持续更新，并通过 SSE 推送
10. 任务结束后生成报告、导出物和历史记录

### 为什么这样设计

- 普通视频是完整文件，可以预先知道总帧数和总时长
- 因此可以做确定性进度条
- 也适合先完整读取元信息，再顺序分析
- 与 RTSP 相比，普通视频不需要重连和实时录制逻辑

### 普通视频的关键实现点

- 上传只负责落盘，不负责识别
- 真正分析发生在任务启动之后
- 分析结果必须来自上传文件本身，而不是继续依赖已有缓存结果

---

## 4.2 RTSP 处理流程

RTSP 链路是“实时拉流、边收边切、边切边识别”的在线任务。

完整流程如下：

1. 前端提交 RTSP 地址创建 `rtsp` 任务
2. 后端建立 RTSP 连接
3. 持续读取视频帧
4. 同时将原始流录制为本地 mp4
5. 每累计 `5` 帧生成一个基础段
6. 第 `1` 段只缓存
7. 从第 `2` 段开始，使用“上一段 + 当前段”组成 `10` 帧窗口
8. 每生成一个窗口就立即送入 `VADTreeWindowRunner`
9. 识别结果送入事件聚合器
10. 后端持续推送状态、FPS、当前时间点和最新事件
11. 用户停止任务后，后端完成收尾、写导出、写历史

### 为什么这样设计

- RTSP 没有天然“视频结束”边界
- 不能像普通视频那样先拿到全量元数据再分析
- 必须把流拆成小窗口，形成可持续处理的准实时链路
- 同步录制是必要动作，否则无法做回放、切片导出和历史复看

### RTSP 的关键实现点

- 必须有独立的拉流运行时
- 必须支持断流重连
- 必须支持录制本地视频
- 必须支持手动停止并收尾

---

## 4.3 统一窗口规则

两条链路共用同一套窗口规则：

- `segment_frames = 5`
- `window_frames = 10`
- 第一个基础段不触发识别
- 从第二个基础段开始形成分析窗口

统一窗口规则的意义在于：

- 普通视频与 RTSP 的分析单元一致
- 事件模型可以统一
- 导出结构可以统一
- 历史记录结构可以统一

---

## 4.4 VADTreeWindowRunner

`VADTreeWindowRunner` 是本次后端设计中的窗口级识别执行器。

职责：

- 接收一个 `10` 帧窗口
- 将窗口组织成可送入 VADTree 的最小输入
- 执行窗口级识别
- 返回统一识别结果

输出固定为：

- `confidence`
- `behavior_type`
- `reason_text`
- 可选中间文件路径

### 为什么不直接复用当前离线大链路

当前 README 中的完整 VADTree 主流程更偏向整段视频批处理：

- GEBD
- HGTree
- VLM caption
- LLM reasoning
- refine
- correlation

它适合整段视频或数据集级推理，不适合直接作为 RTSP 流式运行时。

因此，后端层面需要一个“窗口级 VADTree 执行器”，把实时流输入规整成 VADTree 可接受的最小识别单元，而不是把整个 RTSP 流硬塞进现有离线全流程。

---

## 4.5 事件生成规则

窗口结果不能直接原样暴露给前端，必须先聚合成事件。

统一规则如下：

- `confidence < 0.50`
  - 不生成事件
- `0.50 <= confidence < 0.75`
  - 生成 `medium` 事件
- `confidence >= 0.75`
  - 生成 `high` 事件

相邻窗口若同时满足以下条件，则自动合并：

- `behavior_type` 一致
- 时间连续或间隔可忽略

合并后的事件规则：

- `start_sec` 取最早窗口开始时间
- `end_sec` 取最晚窗口结束时间
- `confidence` 取合并窗口中的最大值
- `reason_text` 取峰值窗口对应说明

这样可以避免：

- 每个窗口都变成一条独立告警
- 前端日志数量过多
- 实时告警抖动过强

---

## 4.6 任务字段统一

为了支撑双输入后端运行态，任务对象需要统一补充以下字段：

- `segment_frames`
- `window_frames`
- `processed_segments`
- `analyzed_windows`
- `buffered_segments`
- `source_fps`
- `processing_fps`
- `stream_state`

其中：

- `source_fps`
  - 表示视频源本身的帧率
- `processing_fps`
  - 表示后端窗口分析速度
- `current_fps`
  - 为兼容前端保留，语义等于 `processing_fps`

### progress_mode

- 普通视频：`determinate`
- RTSP：`indeterminate`

原因很直接：

- 普通视频有总时长和总段数，可计算百分比
- RTSP 是持续输入，不适合做固定完成度

---

## 4.7 SSE 运行态设计

SSE 是比赛演示和前端实时状态的关键。

后端通过 `GET /jobs/{job_id}/stream` 持续推送：

- 当前任务状态
- 当前时间点
- 已处理基础段数
- 已分析窗口数
- 当前 FPS
- 当前延迟
- 当前事件数
- 最新事件列表

### 设计原则

- 普通视频和 RTSP 使用同一个 SSE 通道
- RTSP 优先展示流式状态
- 普通视频优先展示进度与阶段结果
- 一旦任务进入终态，SSE 自动结束

---

## 4.8 导出与历史闭环

上传视频和 RTSP 必须共用同一事件模型、导出模型、历史模型。

统一导出物：

- `events.current.json`
- `events.current.csv`
- `analysis.current.json`
- `clips_manifest.json`
- `report_bundle.zip`

### 切片导出规则

所有切片都从本地视频源裁剪：

- 上传视频：从上传文件裁剪
- RTSP：从录制 mp4 裁剪

这样做的原因是：

- 可重复导出
- 可历史复看
- 不依赖在线流回放

### 历史记录规则

任务完成或收尾后，写入历史记录：

- 普通视频任务写入历史
- RTSP 停止后写入历史
- 历史中可区分来源类型、报告路径和导出物

---

## 4.9 异常处理设计

为了比赛现场稳定，后端必须把异常路径也纳入设计。

### 上传视频异常

- 文件为空
- 文件类型非法
- 文件落盘失败
- 视频无法解码

### RTSP 异常

- 地址为空
- 连接失败
- 断流
- 重连失败
- 停止收尾失败

### 任务异常

- 任务不存在
- 任务已取消
- 导出时任务尚未完成
- 编辑事件时任务或事件不存在

### 统一原则

- 所有错误都返回明确错误信息
- 后端不要只抛通用 500
- SSE 终止前应尽量写入最终错误状态

---

## 5. 与接口文档的分工

`后端,md` 和 [接口.md](./接口.md) 的分工必须明确。

### 后端,md 负责

- 解释为什么这样设计
- 说明模块职责
- 说明双输入数据流
- 说明窗口化识别思路
- 说明状态维护、导出、历史与异常处理的闭环逻辑

### 接口.md 负责

- URL 列表
- 请求参数
- 响应 JSON
- 状态字段
- 错误返回格式

### 文档协作原则

`后端,md` 不再重复大段接口示例，只在必要处引用 `接口.md`。

---

## 6. 落地顺序

后端建议按以下顺序实现。

### 第一阶段：补齐任务模型

目标：

- 先让双输入所需字段进入统一 job 结构
- 让前端和 SSE 能承载新状态

输出：

- 扩展后的 `AnalysisJob`
- 扩展后的 SSE 载荷

### 第二阶段：打通普通视频真实分析链路

目标：

- 上传后能对新视频本身做真实处理
- 不再只依赖已有缓存结果

输出：

- 视频读取
- 5 帧切段
- 10 帧窗口构造
- 窗口识别
- 事件聚合

### 第三阶段：打通 RTSP 链路

目标：

- 建立真正的 RTSP 运行时

输出：

- 拉流
- 录制
- 切段
- 窗口识别
- 断流重连
- 停止收尾

### 第四阶段：导出与历史闭环

目标：

- 让两类输入都能进入统一报告和导出体系

输出：

- JSON / CSV
- 切片
- ZIP
- 历史任务记录

### 第五阶段：联调与稳定性修正

目标：

- 跑通完整演示链路
- 修正任务取消、异常状态和导出边界问题

---

## 7. 验收标准

文档和实现应当共同满足以下标准。

### 7.1 文档层验收

- 明确区分“当前已实现”与“待实现”
- 明确区分“普通视频链路”与“RTSP 链路”
- 明确说明与 `接口.md` 的职责边界

### 7.2 技术一致性验收

- 文档中的字段、状态和规则与 [接口.md](./接口.md) 保持一致
- 文档中的现状描述与 [VADTree/campus_demo/app.py](./VADTree/campus_demo/app.py) 当前实现一致

### 7.3 场景验收

必须至少覆盖以下场景：

#### 普通视频

- 上传成功
- 创建分析任务
- 完成切段和窗口识别
- 生成事件
- 导出结果
- 写入历史

#### RTSP

- 成功连接
- 切段
- 形成窗口
- 实时推送 SSE
- 手动停止
- 完成导出
- 写入历史

#### 异常场景

- 上传失败
- RTSP 断流
- 任务取消
- 导出失败

---

## 8. 结论

这次后端建设的关键，不是新增更多接口，而是把现有 `campus_demo` 从“已有结果演示型后端”升级成“真实双输入分析型后端”。

核心动作只有三件事：

1. 让普通上传视频能够被真实读取、切段、组窗口并识别
2. 让 RTSP 真正完成拉流、录制、切段、窗口识别和实时推送
3. 让两条链路共用同一个事件、导出和历史闭环

只要这三件事完成，当前后端就能支撑比赛所需的双输入演示主链路。
